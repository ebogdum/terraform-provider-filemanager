---
page_title: "filemanager_directory Data Source - filemanager"
subcategory: ""
description: |-
  Lists directory contents from the local filesystem or a configured backend and returns file metadata.
---

# filemanager_directory (Data Source)

## Example Usage

```terraform
# Read directory information
data "filemanager_directory" "app" {
  path = "/var/app"
}

output "directory_info" {
  value = {
    exists      = data.filemanager_directory.app.exists
    permissions = data.filemanager_directory.app.permission
    files       = data.filemanager_directory.app.files
    directories = data.filemanager_directory.app.directories
  }
}

# Read remote directory
data "filemanager_directory" "remote" {
  path    = "/opt/app"
  service = filemanager_ssh_service.server.name
}

# Use in conditionals
resource "filemanager_directory" "app" {
  count          = data.filemanager_directory.app.exists ? 0 : 1
  path           = "/var/app"
  permission     = "0755"
  create_parents = true
}
```

Lists directory contents from the local filesystem or a configured backend and returns file metadata.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path of the directory to list.

### Optional

- `pattern` (String) Glob pattern to filter files (e.g., "*.json").
- `recursive` (Boolean) Whether to list files recursively.
- `service` (String) Service to use for file operations. Defaults to local filesystem.

### Read-Only

- `file_count` (Number) Number of files found.
- `files` (Attributes List) List of files in the directory. (see [below for nested schema](#nestedatt--files))
- `id` (String) Unique identifier for the data source.
- `total_size` (Number) Total size of all files in bytes.

<a id="nestedatt--files"></a>
### Nested Schema for `files`

Read-Only:

- `is_dir` (Boolean) Whether the entry is a directory.
- `mod_time` (String) File modification time in RFC3339 format.
- `mode` (String) File permission mode in octal format.
- `name` (String) Name of the file.
- `path` (String) Full path of the file.
- `size` (Number) Size of the file in bytes.
