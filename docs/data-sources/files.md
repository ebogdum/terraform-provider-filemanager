---
page_title: "filemanager_files Data Source - filemanager"
subcategory: ""
description: |-
  Lists files in a directory matching a glob pattern.
---

# filemanager_files (Data Source)

## Example Usage

```terraform
# List files matching a pattern
data "filemanager_files" "configs" {
  path    = "/etc/app"
  pattern = "*.json"
}

output "config_files" {
  value = data.filemanager_files.configs.files
}

# List files recursively
data "filemanager_files" "all_logs" {
  path      = "/var/log"
  pattern   = "*.log"
  recursive = true
}

# List files on remote server
data "filemanager_files" "remote_configs" {
  path    = "/etc/app"
  pattern = "*.yaml"
  service = filemanager_ssh_service.server.name
}

# Use file list in for_each
data "filemanager_checksum" "config_checksums" {
  for_each = toset(data.filemanager_files.configs.files)
  path     = each.value
}
```

Lists files in a directory matching a glob pattern.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The directory path to search in.

### Optional

- `pattern` (String) Glob pattern to match files (e.g., '*.txt', '*.conf').
- `recursive` (Boolean) Search subdirectories recursively.
- `service` (String) Service to use for file operations. Defaults to local filesystem.

### Read-Only

- `file_count` (Number) Number of files matching the pattern.
- `files` (Attributes List) List of files matching the pattern. (see [below for nested schema](#nestedatt--files))
- `id` (String) Unique identifier for the data source.

<a id="nestedatt--files"></a>
### Nested Schema for `files`

Read-Only:

- `is_dir` (Boolean) Whether the path is a directory.
- `mod_time` (String) File modification time in RFC3339 format.
- `mode` (String) File permission mode in octal format.
- `name` (String) Base name of the file.
- `path` (String) Full path to the file.
- `size` (Number) Size of the file in bytes.
