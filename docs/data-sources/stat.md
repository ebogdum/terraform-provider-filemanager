---
page_title: "filemanager_stat Data Source - filemanager"
subcategory: ""
description: |-
  Returns detailed file or directory metadata including permissions, ownership, and timestamps.
---

# filemanager_stat (Data Source)

## Example Usage

```terraform
# Get file statistics
data "filemanager_stat" "config" {
  path = "/etc/app/config.json"
}

output "file_info" {
  value = {
    exists       = data.filemanager_stat.config.exists
    is_file      = data.filemanager_stat.config.is_file
    is_directory = data.filemanager_stat.config.is_directory
    is_symlink   = data.filemanager_stat.config.is_symlink
    size         = data.filemanager_stat.config.size
    permissions  = data.filemanager_stat.config.permission
    modified     = data.filemanager_stat.config.modified_time
  }
}

# Check remote file stats
data "filemanager_stat" "remote" {
  path    = "/var/log/app.log"
  service = filemanager_ssh_service.server.name
}

# Use for conditional logic
locals {
  config_exists = data.filemanager_stat.config.exists
  config_size   = data.filemanager_stat.config.size
}
```

Returns detailed file or directory metadata including permissions, ownership, and timestamps.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path to get metadata for.

### Optional

- `accessed_within` (String) Check if file was accessed within this duration (e.g., '30m', '24h', '7d'). Sets is_accessed_within.
- `modified_within` (String) Check if file was modified within this duration (e.g., '30m', '24h', '7d'). Sets is_modified_within.
- `service` (String) Service to use for file operations. Defaults to local filesystem.

### Read-Only

- `access_time` (String) Last access time in RFC3339 format.
- `age` (String) Duration since the file was last modified (e.g., '2h30m15s').
- `content_type` (String) Content type (MIME type) if available.
- `creation_time` (String) Creation time in RFC3339 format (if available).
- `exists` (Boolean) Whether the path exists.
- `gid` (Number) Group ID of the file owner (Unix only).
- `group_name` (String) Group name of the file owner (resolved from GID, Unix only).
- `id` (String) Unique identifier for the data source.
- `is_accessed_within` (Boolean) True if file was accessed within the duration specified by accessed_within. Null if accessed_within not set.
- `is_dir` (Boolean) Whether the path is a directory.
- `is_file` (Boolean) Whether the path is a regular file.
- `is_modified_within` (Boolean) True if file was modified within the duration specified by modified_within. Null if modified_within not set.
- `is_symlink` (Boolean) Whether the path is a symbolic link.
- `link_target` (String) Target of the symbolic link (empty if not a symlink).
- `mod_time` (String) Last modification time in RFC3339 format.
- `mode` (String) File permission mode in octal format.
- `owner_name` (String) Username of the file owner (resolved from UID, Unix only).
- `size` (Number) Size in bytes (0 for directories).
- `uid` (Number) User ID of the file owner (Unix only).
