---
page_title: "filemanager_hcl Data Source - filemanager"
subcategory: ""
description: |-
  Reads an HCL (HashiCorp Configuration Language) file and returns its parsed content as a dynamic value.
---

# filemanager_hcl (Data Source)

## Example Usage

```terraform
# Read and parse HCL file
data "filemanager_hcl" "config" {
  path = "/etc/app/config.hcl"
}

# Access parsed data
output "server_host" {
  value = data.filemanager_hcl.config.data.server.host
}

output "features" {
  value = data.filemanager_hcl.config.data.features
}

# Read HCL from remote server
data "filemanager_hcl" "remote" {
  path    = "/etc/consul.d/config.hcl"
  service = filemanager_ssh_service.server.name
}

# Read Vault configuration
data "filemanager_hcl" "vault" {
  path = "/etc/vault.d/config.hcl"
}

output "vault_storage" {
  value = data.filemanager_hcl.vault.data.storage
}

# Read Nomad configuration
data "filemanager_hcl" "nomad" {
  path = "/etc/nomad.d/config.hcl"
}

output "datacenter" {
  value = data.filemanager_hcl.nomad.data.datacenter
}
```


Reads an HCL (HashiCorp Configuration Language) file and returns its parsed content as a dynamic value.


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path of the HCL file to read.

### Optional

- `service` (String) Service to use for file operations. Defaults to local filesystem.

### Read-Only

- `content` (String, Sensitive) The raw file content as a string.
- `data` (Dynamic) The parsed HCL content as a dynamic Terraform value.
- `id` (String) Unique identifier for the data source.
- `md5` (String) MD5 checksum of the file content.
- `sha256` (String) SHA-256 checksum of the file content.
- `size` (Number) Size of the file in bytes.
