---
page_title: "filemanager_file Data Source - filemanager"
subcategory: ""
description: |-
  Reads a file from the local filesystem or a configured backend and returns its content and metadata.
---

# filemanager_file (Data Source)

## Example Usage

```terraform
# Read a local file
data "filemanager_file" "config" {
  path = "/etc/app/config.txt"
}

output "config_content" {
  value = data.filemanager_file.config.content
}

output "config_sha256" {
  value = data.filemanager_file.config.sha256
}

# Read a file from remote server
data "filemanager_file" "remote_config" {
  path    = "/etc/app/config.txt"
  service = filemanager_ssh_service.server.name
}

# Read base64-encoded content (useful for binary files)
data "filemanager_file" "certificate" {
  path = "/etc/ssl/certs/server.crt"
}

output "cert_base64" {
  value = data.filemanager_file.certificate.content_base64
}
```

Reads a file from the local filesystem or a configured backend and returns its content and metadata.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path of the file to read.

### Optional

- `service` (String) Service to use for file operations. Defaults to local filesystem.

### Read-Only

- `content` (String) The content of the file as a string. Empty if file is binary or too large.
- `content_base64` (String) The content of the file as base64 encoded string.
- `id` (String) Unique identifier for the data source.
- `is_dir` (Boolean) Whether the path is a directory.
- `md5` (String) MD5 checksum of the file content.
- `mod_time` (String) File modification time in RFC3339 format.
- `mode` (String) File permission mode in octal format.
- `sha256` (String) SHA-256 checksum of the file content.
- `size` (Number) Size of the file in bytes.
