---
page_title: "filemanager_env_file Resource - filemanager"
subcategory: ""
description: |-
  Manages a .env file with support for environment variable management.
---

# filemanager_env_file (Resource)

## Example Usage

```terraform
# Create an environment file
resource "filemanager_env_file" "app" {
  path = "/etc/app/.env"
  data = {
    APP_NAME        = "MyApplication"
    APP_ENV         = var.environment
    APP_DEBUG       = var.debug_enabled ? "true" : "false"
    APP_URL         = var.app_url
    DB_HOST         = var.db_host
    DB_PORT         = "5432"
    DB_DATABASE     = var.db_name
    DB_USERNAME     = var.db_user
    DB_PASSWORD     = var.db_password
    REDIS_HOST      = var.redis_host
    REDIS_PORT      = "6379"
    CACHE_DRIVER    = "redis"
    SESSION_DRIVER  = "redis"
    LOG_LEVEL       = "info"
  }

  file_permission    = "0600"
  create_parent_dirs = true
}

# Docker environment file
resource "filemanager_env_file" "docker" {
  path = "${var.project_path}/.env"
  data = {
    COMPOSE_PROJECT_NAME = var.project_name
    DOCKER_REGISTRY      = var.registry_url
    IMAGE_TAG            = var.image_tag
  }

  create_parent_dirs = true
}

# Merge with existing env file
resource "filemanager_env_file" "merged" {
  path = "/etc/app/.env.local"
  data = {
    NEW_VAR = "new_value"
  }

  merge = true
}
```

Manages a .env file with support for environment variable management.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path where the .env file will be created.
- `variables` (Map of String) Map of environment variable names to values.

### Optional

- `atomic_write` (Boolean) Use atomic write operations.
- `create_parent_dirs` (Boolean) Create parent directories if they don't exist.
- `directory_permission` (String) Directory permission mode for parent directories.
- `file_permission` (String) File permission mode in octal format.
- `merge_with` (Map of String) Additional variables to merge with the main content.
- `service` (String) Service to use for file operations. Defaults to local filesystem.
- `sort_keys` (Boolean) Sort variable names alphabetically.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `extension` (String) The file extension without the leading dot (e.g., 'json').
- `filename` (String) The base name of the file (e.g., 'config.json').
- `id` (String) Unique identifier for the resource.
- `md5` (String) MD5 checksum of the file content.
- `rendered` (String, Sensitive) The rendered .env content as a string.
- `sha256` (String) SHA-256 checksum of the file content.
- `size` (Number) Size of the file in bytes.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_env_file.app /etc/app/.env
```
