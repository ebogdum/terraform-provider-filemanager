---
page_title: "filemanager_toml_file Resource - filemanager"
subcategory: ""
description: |-
  Manages a TOML file with support for deep merge and structured content manipulation.
---

# filemanager_toml_file (Resource)

## Example Usage

```terraform
# Create a TOML configuration file
resource "filemanager_toml_file" "config" {
  path = "/etc/app/config.toml"
  data = {
    title = "Application Config"
    owner = {
      name  = "DevOps Team"
      email = "devops@example.com"
    }
    database = {
      server         = var.db_server
      ports          = [8001, 8002, 8003]
      connection_max = 5000
      enabled        = true
    }
  }

  create_parent_dirs = true
}

# Create Cargo.toml for Rust project
resource "filemanager_toml_file" "cargo" {
  path = "${var.project_path}/Cargo.toml"
  data = {
    package = {
      name    = var.package_name
      version = var.package_version
      edition = "2021"
    }
    dependencies = {
      serde = { version = "1.0", features = ["derive"] }
      tokio = { version = "1", features = ["full"] }
    }
  }

  create_parent_dirs = true
}
```

Manages a TOML file with support for deep merge and structured content manipulation.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `content` (Map of String) The TOML content as a map.
- `path` (String) The path where the TOML file will be created.

### Optional

- `atomic_write` (Boolean) Use atomic write operations.
- `create_parent_dirs` (Boolean) Create parent directories if they don't exist.
- `directory_permission` (String) Directory permission mode for parent directories.
- `file_permission` (String) File permission mode in octal format.
- `merge_strategy` (String) Merge strategy: replace, deep, append, concat, union.
- `merge_with` (Map of String) Additional content to merge with the main content.
- `service` (String) Service to use for file operations. Defaults to local filesystem.
- `sort_keys` (Boolean) Sort TOML keys alphabetically.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `extension` (String) The file extension without the leading dot (e.g., 'json').
- `filename` (String) The base name of the file (e.g., 'config.json').
- `id` (String) Unique identifier for the resource.
- `md5` (String) MD5 checksum of the file content.
- `rendered` (String, Sensitive) The rendered TOML content as a string.
- `sha256` (String) SHA-256 checksum of the file content.
- `size` (Number) Size of the file in bytes.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_toml_file.config /etc/app/config.toml
```
