---
page_title: "filemanager_sensitive_file Resource - filemanager"
subcategory: ""
description: |-
  Manages a file with sensitive content. The content is marked as sensitive and will not appear in logs or plan output.
Security Notes
Content is marked as sensitive and won't appear in logs or plan outputState still contains the content, so protect your state file appropriatelyConsider using restrictive file permissions (e.g., "0600")
---

# filemanager_sensitive_file (Resource)

## Example Usage

```terraform
# Create a file with sensitive content (masked in logs/state)
resource "filemanager_sensitive_file" "credentials" {
  path    = "/etc/app/credentials.json"
  content = jsonencode({
    api_key    = var.api_key
    api_secret = var.api_secret
  })

  file_permission    = "0600"
  create_parent_dirs = true
}

# Create SSH private key file
resource "filemanager_sensitive_file" "ssh_key" {
  path    = "/home/deploy/.ssh/id_rsa"
  content = var.ssh_private_key

  file_permission    = "0600"
  create_parent_dirs = true
}

# Create TLS certificate key
resource "filemanager_sensitive_file" "tls_key" {
  path           = "/etc/ssl/private/server.key"
  content_base64 = var.tls_key_base64

  file_permission    = "0600"
  create_parent_dirs = true
}
```


Manages a file with sensitive content. The content is marked as sensitive and will not appear in logs or plan output.

## Security Notes

- Content is marked as sensitive and won't appear in logs or plan output
- State still contains the content, so protect your state file appropriately
- Consider using restrictive file permissions (e.g., "0600")


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path where the file will be created.

### Optional

- `atomic_write` (Boolean) Use atomic write operations.
- `content` (String, Sensitive) The sensitive content to write to the file as a string. Use jsonencode() for JSON content.
- `content_base64` (String, Sensitive) The sensitive content to write to the file, base64 encoded.
- `create_parent_dirs` (Boolean) Create parent directories if they don't exist.
- `directory_permission` (String) Directory permission mode for parent directories.
- `file_permission` (String) File permission mode in octal format. Defaults to 0600 for security.
- `indent` (Number) Indentation spaces for JSON output when pretty_print_json is true. Defaults to 2.
- `pretty_print_json` (Boolean) If true and content is valid JSON, it will be pretty-printed with indentation.
- `service` (String) Service to use for file operations. Defaults to local filesystem.
- `sort_keys` (Boolean) Sort JSON object keys alphabetically when pretty_print_json is true.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `extension` (String) The file extension without the leading dot (e.g., 'json').
- `filename` (String) The base name of the file (e.g., 'config.json').
- `id` (String) Unique identifier for the resource.
- `md5` (String) Deprecated insecure checksum field. Always null.
- `sha256` (String, Sensitive) SHA-256 checksum of the file content.
- `size` (Number) Size of the file in bytes.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_sensitive_file.credentials /etc/app/credentials.json
```
