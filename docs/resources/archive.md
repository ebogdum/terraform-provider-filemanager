---
page_title: "filemanager_archive Resource - filemanager"
subcategory: ""
description: |-
  Creates an archive file from a source directory or list of files.
---

# filemanager_archive (Resource)

## Example Usage

```terraform
# Create a ZIP archive from a directory
resource "filemanager_archive" "app_bundle" {
  path       = "/var/releases/app-${var.version}.zip"
  source_dir = "/var/www/app"
  type       = "zip"

  excludes = [
    "*.log",
    "*.tmp",
  ]

  create_parent_dirs = true
}

# Create a tar.gz archive
resource "filemanager_archive" "backup" {
  path       = "/var/backups/config.tar.gz"
  source_dir = "/etc/app"
  type       = "tar.gz"

  create_parent_dirs = true
}

# Create archive from specific files
resource "filemanager_archive" "logs" {
  path = "/var/archives/logs.zip"
  type = "zip"

  source_files = [
    "/var/log/app/app.log",
    "/var/log/app/error.log",
    "/var/log/nginx/access.log",
  ]

  create_parent_dirs = true
}
```

Creates an archive file from a source directory or list of files.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path where the archive will be created.
- `type` (String) Archive type: zip, tar, tar.gz.

### Optional

- `create_parent_dirs` (Boolean) Create parent directories if they don't exist.
- `directory_permission` (String) Directory permission mode for parent directories.
- `excludes` (List of String) Glob patterns to exclude from the archive.
- `file_permission` (String) File permission mode for the archive.
- `source_dir` (String) Source directory to archive.
- `source_files` (List of String) List of source file paths to include in the archive.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `extension` (String) The file extension without the leading dot.
- `file_count` (Number) Number of files in the archive.
- `filename` (String) The base name of the file.
- `id` (String) Unique identifier for the resource.
- `md5` (String) Deprecated insecure checksum field. Always null.
- `sha256` (String) SHA-256 checksum of the archive.
- `size` (Number) Size of the archive in bytes.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_archive.app_bundle /var/releases/app-1.0.0.zip
```
