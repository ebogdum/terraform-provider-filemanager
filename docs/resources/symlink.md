---
page_title: "filemanager_symlink Resource - filemanager"
subcategory: ""
description: |-
  Manages a symbolic link on the filesystem.
---

# filemanager_symlink (Resource)

## Example Usage

```terraform
# Create a symbolic link
resource "filemanager_symlink" "config" {
  path   = "/etc/app/current-config"
  target = "/etc/app/config-v2.json"
}

# Create symlink for versioned deployment
resource "filemanager_symlink" "current_release" {
  path   = "/var/www/current"
  target = "/var/www/releases/${var.release_version}"

  create_parent_dirs = true
}

# Create symlink on remote server
resource "filemanager_symlink" "remote_link" {
  path    = "/opt/app/current"
  target  = "/opt/app/releases/latest"
  service = filemanager_ssh_service.server.id
}
```

Manages a symbolic link on the filesystem.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path of the symbolic link to create.
- `target` (String) The target path that the symlink points to.

### Optional

- `create_parent_dirs` (Boolean) Create parent directories if they don't exist.
- `force` (Boolean) Remove existing file/symlink at path before creating.
- `service` (String) Service to use for file operations. Defaults to local filesystem.
- `target_type` (String) How to treat the target path: 'absolute' resolves relative targets to absolute paths (symlink breaks if directory tree moves), 'relative' keeps the target as-is (symlink is portable). Defaults to 'absolute'.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `exists` (Boolean) Whether the symbolic link exists.
- `extension` (String) The file extension without the leading dot.
- `filename` (String) The base name of the symlink.
- `id` (String) Unique identifier for the resource.
- `resolved_path` (String) The fully resolved target path.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_symlink.config /etc/app/current-config
```
