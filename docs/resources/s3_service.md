---
page_title: "filemanager_s3_service Resource - filemanager"
subcategory: ""
description: |-
  Configures an S3-compatible storage service for use with other filemanager resources.
---

# filemanager_s3_service (Resource)

## Example Usage

```terraform
# Configure S3 service for AWS
resource "filemanager_s3_service" "main" {
  name   = "aws-main"
  bucket = var.s3_bucket
  region = var.aws_region

  # Credentials from environment or instance profile
}

# S3 service with explicit credentials
resource "filemanager_s3_service" "backup" {
  name   = "aws-backup"
  bucket = var.backup_bucket
  region = var.aws_region

  access_key = var.aws_access_key
  secret_key = var.aws_secret_key
}

# S3-compatible service (MinIO, DigitalOcean Spaces, etc.)
resource "filemanager_s3_service" "minio" {
  name     = "minio-storage"
  bucket   = "my-bucket"
  endpoint = "https://minio.example.com"
  region   = "us-east-1"

  access_key       = var.minio_access_key
  secret_key       = var.minio_secret_key
  force_path_style = true
}

# DigitalOcean Spaces
resource "filemanager_s3_service" "spaces" {
  name     = "do-spaces"
  bucket   = var.spaces_bucket
  endpoint = "https://${var.spaces_region}.digitaloceanspaces.com"
  region   = var.spaces_region

  access_key = var.spaces_access_key
  secret_key = var.spaces_secret_key
}
```

Configures an S3-compatible storage service for use with other filemanager resources.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `bucket` (String) The S3 bucket name.

### Optional

- `access_key` (String, Sensitive) AWS access key ID. If not provided, uses default AWS credential chain.
- `access_key_file` (String) Path to file containing AWS access key ID. Alternative to access_key.
- `base_path` (String) Base path prefix for all operations within the bucket.
- `endpoint` (String) Custom endpoint URL for S3-compatible services (e.g., MinIO, DigitalOcean Spaces).
- `region` (String) AWS region for the bucket. Defaults to us-east-1.
- `secret_key` (String, Sensitive) AWS secret access key. If not provided, uses default AWS credential chain.
- `secret_key_file` (String) Path to file containing AWS secret access key. Alternative to secret_key.
- `session_token` (String, Sensitive) AWS session token for temporary credentials.

### Read-Only

- `connected` (Boolean) Whether the service is currently connected.
- `id` (String) Unique identifier for the service (format: s3:region:bucket).
- `service_type` (String) The type of service (s3, gcs, azure, b2, or ssh).

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_s3_service.main aws-main
```
