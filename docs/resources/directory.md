---
page_title: "filemanager_directory Resource - filemanager"
subcategory: ""
description: |-
  Manages a directory on the filesystem or remote backend.
---

# filemanager_directory (Resource)

## Example Usage

```terraform
# Create a directory with specific permissions
resource "filemanager_directory" "app" {
  path           = "/var/app/data"
  permission     = "0755"
  create_parents = true
}

# Create multiple directories with ownership
resource "filemanager_directory" "logs" {
  path           = "/var/log/myapp"
  permission     = "0750"
  create_parents = true
}

# Create directory on remote server
resource "filemanager_directory" "remote_app" {
  path           = "/opt/myapp"
  permission     = "0755"
  create_parents = true
  service        = filemanager_ssh_service.server.name
}
```

Manages a directory on the filesystem or remote backend.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path of the directory to manage.

### Optional

- `create_parents` (Boolean) Create parent directories if they don't exist.
- `force_delete` (Boolean) Delete directory even if not empty (recursive delete).
- `gid` (Number) Group ID for directory ownership (Unix only).
- `group` (String) Group name for directory ownership. Alternative to gid.
- `owner` (String) User name for directory ownership. Alternative to uid.
- `permission` (String) Permission mode in octal format (e.g., "0755").
- `service` (String) Service to use for operations. Defaults to local filesystem.
- `uid` (Number) User ID for directory ownership (Unix only).

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `exists` (Boolean) Whether the directory exists.
- `extension` (String) The file extension without the leading dot (empty for directories).
- `filename` (String) The base name of the directory.
- `id` (String) Unique identifier for the resource.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_directory.app /var/app/data
```
