---
page_title: "filemanager_transfer Resource - filemanager"
subcategory: ""
description: |-
  Transfers files or directories between any two backends.
---

# filemanager_transfer (Resource)

## Example Usage

```terraform
# Transfer files between different backends
resource "filemanager_transfer" "s3_to_server" {
  source          = "backups/latest.sql.gz"
  source_service  = filemanager_s3_service.backups.name
  destination     = "/var/backups/database.sql.gz"
  dest_service    = filemanager_ssh_service.server.name

  verify_checksum = true
}

# Transfer from Azure to S3
resource "filemanager_transfer" "azure_to_s3" {
  source          = "exports/data.csv"
  source_service  = filemanager_azure_service.source.name
  destination     = "imports/data.csv"
  dest_service    = filemanager_s3_service.destination.name

  verify_checksum = true
}

# Transfer between servers
resource "filemanager_transfer" "server_to_server" {
  source          = "/var/data/important.db"
  source_service  = filemanager_ssh_service.primary.name
  destination     = "/var/data/replica.db"
  dest_service    = filemanager_ssh_service.secondary.name

  verify_checksum = true
}

# Transfer from GCS to local
resource "filemanager_transfer" "gcs_to_local" {
  source         = "artifacts/build-${var.build_number}.tar.gz"
  source_service = filemanager_gcs_service.artifacts.name
  destination    = "/var/releases/build-${var.build_number}.tar.gz"

  verify_checksum    = true
  create_parent_dirs = true
}
```

Transfers files or directories between any two backends.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `destination_backend` (String) Destination backend alias.
- `destination_path` (String) Destination path within the destination backend.
- `source_backend` (String) Source backend alias.
- `source_path` (String) Source path (file or directory) within the source backend.

### Optional

- `buffer_size_kb` (Number) Buffer size in KB for transfer operations.
- `checksum_verify` (Boolean) Verify checksums after transfer.
- `concurrency` (Number) Number of parallel transfer workers.
- `excludes` (List of String) Glob patterns to exclude from the transfer.
- `includes` (List of String) Glob patterns to include in the transfer.
- `overwrite` (Boolean) Overwrite existing files at destination.
- `preserve_permissions` (Boolean) Preserve file permissions.
- `preserve_timestamps` (Boolean) Preserve modification timestamps.
- `recursive` (Boolean) Transfer directories recursively.
- `streaming` (Boolean) Use streaming mode for transfers.
- `triggers` (Map of String) Map of values that, when changed, trigger a re-transfer.
- `zero_copy` (Boolean) Attempt zero-copy transfers where supported.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `bytes_transferred` (Number) Total bytes transferred.
- `directory` (String) The parent directory of the path.
- `duration_ms` (Number) Duration of the transfer operation in milliseconds.
- `extension` (String) The file extension without the leading dot.
- `filename` (String) The base name of the file.
- `files_failed` (Number) Number of files that failed to transfer.
- `files_transferred` (Number) Number of files transferred successfully.
- `id` (String) Unique identifier for the resource.
- `transfer_method` (String) Method used for transfer (direct, parallel_chunked, kernel_zero_copy, streaming_buffer, standard).

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_transfer.s3_to_server /var/backups/database.sql.gz
```
