---
page_title: "filemanager_yaml_file Resource - filemanager"
subcategory: ""
description: |-
  Manages a YAML file with support for deep merge and structured content manipulation.
---

# filemanager_yaml_file (Resource)

## Example Usage

```terraform
# Create a YAML configuration file
resource "filemanager_yaml_file" "config" {
  path = "/etc/app/config.yaml"
  data = {
    app_name    = "MyApplication"
    environment = var.environment
    database = {
      host     = var.db_host
      port     = 5432
      pool_size = 10
    }
    logging = {
      level   = "info"
      format  = "json"
      outputs = ["stdout", "file"]
    }
  }

  create_parent_dirs = true
}

# Merge with existing YAML file
resource "filemanager_yaml_file" "kubernetes_config" {
  path = "/etc/kubernetes/config.yaml"
  data = {
    spec = {
      replicas = var.replicas
    }
  }

  merge      = true
  deep_merge = true
}

# YAML file with multi-line strings
resource "filemanager_yaml_file" "playbook" {
  path = "/etc/ansible/playbook.yaml"
  data = {
    hosts = "all"
    tasks = [
      {
        name   = "Install packages"
        apt    = { name = ["nginx", "python3"], state = "present" }
      }
    ]
  }

  create_parent_dirs = true
}
```

Manages a YAML file with support for deep merge and structured content manipulation.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `content` (String) The YAML content as a string. Use yamlencode() to convert HCL to YAML.
- `path` (String) The path where the YAML file will be created.

### Optional

- `atomic_write` (Boolean) Use atomic write operations.
- `create_parent_dirs` (Boolean) Create parent directories if they don't exist.
- `directory_permission` (String) Directory permission mode for parent directories.
- `file_permission` (String) File permission mode in octal format.
- `indent` (Number) Indentation spaces for YAML output.
- `merge_strategy` (String) Merge strategy: replace, deep, append, concat, union.
- `merge_with` (String) Additional YAML content to merge with the main content. Use yamlencode() to convert HCL to YAML.
- `service` (String) Service to use for file operations. Defaults to local filesystem.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `extension` (String) The file extension without the leading dot (e.g., 'json').
- `filename` (String) The base name of the file (e.g., 'config.json').
- `id` (String) Unique identifier for the resource.
- `md5` (String) Deprecated insecure checksum field. Always null.
- `rendered` (String, Sensitive) The rendered YAML content as a string.
- `sha256` (String) SHA-256 checksum of the file content.
- `size` (Number) Size of the file in bytes.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_yaml_file.config /etc/app/config.yaml
```
