---
page_title: "filemanager_ini_file Resource - filemanager"
subcategory: ""
description: |-
  Manages an INI configuration file with support for sections and key-value pairs.
---

# filemanager_ini_file (Resource)

## Example Usage

```terraform
# Create an INI configuration file
resource "filemanager_ini_file" "config" {
  path = "/etc/app/config.ini"
  data = {
    general = {
      app_name = "MyApplication"
      version  = "1.0.0"
      debug    = "false"
    }
    database = {
      host     = var.db_host
      port     = "5432"
      username = var.db_user
      password = var.db_password
    }
    logging = {
      level  = "INFO"
      file   = "/var/log/app.log"
      format = "json"
    }
  }

  create_parent_dirs = true
}

# Create PHP configuration
resource "filemanager_ini_file" "php" {
  path = "/etc/php/8.2/fpm/conf.d/custom.ini"
  data = {
    PHP = {
      memory_limit        = "256M"
      upload_max_filesize = "64M"
      post_max_size       = "64M"
      max_execution_time  = "300"
    }
  }

  create_parent_dirs = true
}

# Merge with existing INI file
resource "filemanager_ini_file" "merged" {
  path = "/etc/app/settings.ini"
  data = {
    new_section = {
      key = "value"
    }
  }

  merge = true
}
```

Manages an INI configuration file with support for sections and key-value pairs.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path where the INI file will be created.
- `sections` (Map of String) Map of section names to JSON-encoded key-value pairs.

### Optional

- `atomic_write` (Boolean) Use atomic write operations.
- `create_parent_dirs` (Boolean) Create parent directories if they don't exist.
- `directory_permission` (String) Directory permission mode for parent directories.
- `file_permission` (String) File permission mode in octal format.
- `merge_strategy` (String) Merge strategy: replace, deep.
- `merge_with` (Map of String) Additional sections to merge with the main content.
- `service` (String) Service to use for file operations. Defaults to local filesystem.
- `sort_keys` (Boolean) Sort section and key names alphabetically.

### Read-Only

- `absolute_path` (String) The absolute resolved path.
- `directory` (String) The parent directory of the path.
- `extension` (String) The file extension without the leading dot (e.g., 'json').
- `filename` (String) The base name of the file (e.g., 'config.json').
- `id` (String) Unique identifier for the resource.
- `md5` (String) MD5 checksum of the file content.
- `rendered` (String, Sensitive) The rendered INI content as a string.
- `sha256` (String) SHA-256 checksum of the file content.
- `size` (Number) Size of the file in bytes.

## Import

Import is supported using the following syntax:

```shell
terraform import filemanager_ini_file.config /etc/app/config.ini
```
