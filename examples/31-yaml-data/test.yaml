server:
  host: 0.0.0.0
  port: 8080
  timeout: 30s

logging:
  level: info
  format: json
  output: stdout

database:
  driver: postgresql
  connection:
    host: db.example.com
    port: 5432
    database: production

features:
  - authentication
  - authorization
  - rate_limiting
